apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "eventticket-api.fullname" . }}-test-connection"
  labels:
    {{- include "eventticket-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "eventticket-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
