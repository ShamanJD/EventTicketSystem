apiVersion: v1
kind: Service
metadata:
  name: {{ include "eventticket-api.fullname" . }}
  labels:
    {{- include "eventticket-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "eventticket-api.selectorLabels" . | nindent 4 }}
